server {
    listen 80;
    access_log  /var/log/nginx/80.a.log main;
    error_log   /var/log/nginx/80.e.log notice;

    root /var/www/html;
    index index.html index.htm index.php;

    server_name localhost;

    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
        # Uncomment to enable naxsi on this location
        # include /etc/nginx/naxsi.rules
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}

server {
    listen 8082;
    access_log  /var/log/nginx/8082.a.log main;
    error_log   /var/log/nginx/8082.e.log notice;

    root /var/www/html/colourfulRMS/www;
    index index.html index.htm index.php;

    #server_name dongfang.wooyou.com.cn;
    server_name zytest.colourlife.net;

    location ~ /\.  { deny all; }

    if (!-e $request_filename) {
        rewrite  ^(.*)$  /index.php  last;
        break;
    }
    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
        # Uncomment to enable naxsi on this location
        # include /etc/nginx/naxsi.rules
    }
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}

server {
    listen 8081;
    access_log  /var/log/nginx/8081.a.log main;
    error_log   /var/log/nginx/8081.e.log notice;

    root /var/www/html/iceapi;
    index index.html index.htm index.php;

    #server_name dongfang.wooyou.com.cn;

    location ~ /\.  { deny all; }

    if (!-e $request_filename) {
        rewrite  ^(.*)$  /index.php  last;
        break;
    }
    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
        # Uncomment to enable naxsi on this location
        # include /etc/nginx/naxsi.rules
    }
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}

server {
    listen 8180;
    access_log  /var/log/nginx/iceadmin.8180.a.log main;
    error_log   /var/log/nginx/iceadmin.8180.e.log notice;

    root /var/www/html/iceadmin/www;
    index index.html index.htm index.php;

    #server_name iceadmintest.colourlife.net;

    location ~ /\.  { deny all; }

    if (!-e $request_filename) {
        rewrite  ^(.*)$  /index.php  last;
        break;
    }
    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
        # Uncomment to enable naxsi on this location
        # include /etc/nginx/naxsi.rules
    }
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}

server {
    listen 8484;
    access_log  /var/log/nginx/8484.a.log main;
    error_log   /var/log/nginx/8484.e.log notice;

    root /var/www/html/casems/www;
    index index.html index.htm index.php;

    #server_name dongfang.wooyou.com.cn;

    location ~ /\.  { deny all; }

    if (!-e $request_filename) {
        rewrite  ^(.*)$  /index.php  last;
        break;
    }
    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
        # Uncomment to enable naxsi on this location
        # include /etc/nginx/naxsi.rules
    }
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}

server {
    listen 5566;
    access_log  /var/log/nginx/5566.a.log main;
    error_log   /var/log/nginx/5566.e.log notice;

    root /var/www/html/webgrind;
    index index.html index.htm index.php;

    location ~ /\.  { deny all; }

    if (!-e $request_filename) {
        rewrite  ^(.*)$  /index.php  last;
        break;
    }
    location / {
        try_files $uri $uri/ =404;
    }
    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}
